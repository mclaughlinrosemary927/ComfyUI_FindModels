# CODEX_START_HERE.md

把下面这段作为新电脑 Codex 新会话的第一条消息使用。  
注意：这不会还原旧聊天气泡；它会让新会话读取仓库中的上下文文件并接上项目状态。

```text
继续这个项目。先读取 AGENTS.md、PROJECT_CONTEXT.md、CONVERSATION_CONTEXT.md 和当前源码，理解项目背景、历史问题、用户偏好和安全规则，然后检查 git 状态。
```

如果需要更完整的历史摘要，继续读取 `PROJECT_CONVERSATION_SUMMARY.md`。
